node_modules
dist
build
coverage
pnpm-lock.yaml
directus/snapshot
playwright-report
test-results

# Generated / tooling docs (not hand-maintained source)
specs/
.specify/
.claude/
CLAUDE.md

# Third-party reference material (the operations manager's own CRM + its data).
# Never reformat it: it is evidence, and its exact bytes are what we compare
# our import against. It is also gitignored — see .gitignore.
Ayman/
